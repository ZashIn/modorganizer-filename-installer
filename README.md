Mod Organizer plugin (`IPluginInstaller`) to set the mod name (`GuessedString`) to the filename. Actual installation has to be handled by other installers.

## Installation
Drop `filename_installer.py` into MO's plugins directory.